# 安全说明

## 报告问题

请优先使用 GitHub 仓库的 Security Advisories 私下报告可能泄露 API Key、绕过数据校验或执行非预期脚本的安全问题。不要在公开 Issue 中附上真实凭据、个人学习资料或完整浏览器备份。

如果暂时无法使用私密报告，请创建不含利用细节和敏感数据的 Issue，说明需要与维护者私下联系。

## 使用边界

- API Key 保存在浏览器 IndexedDB 中，但网页仍可在当前页面上下文中读取并用于发起请求；请只在可信部署上使用。
- 公共体验站使用 HTTP 时，不建议输入长期有效的 API Key。正式部署应启用 HTTPS。
- 导入的 DOCX、PDF、图片、文本和 JSON 应来自可信来源，并在 AI 审核页确认结果。
- 请定期导出 JSON 备份。浏览器存储不等同于云端备份，也不会跨地址自动迁移。

目前仅维护最新发布版本的安全修复。
